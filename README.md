# 🐦 呆呆鸟酒馆启动助手

SillyTavern 启动器，支持版本管理、速度优化和API聚合功能。

## 一键安装

```bash
curl -fsSL https://raw.githubusercontent.com/2830897438/st-launcher/main/install.sh | bash
```

## 功能特性

- 🎯 **版本管理** - 一键安装/切换/卸载多个SillyTavern版本
- ⚡ **速度优化** - 禁用缓存破坏器，添加静态资源缓存
- 🔗 **API聚合** - 自动轮询多个API密钥，跳过不可用密钥
- 📱 **Termux支持** - 完美适配Android Termux环境
- 🖥️ **本地运行** - 所有服务都在本地运行，无需公网IP

## 使用方法

### 启动

```bash
daidai
```

### 访问

启动后在浏览器中打开：
- **启动器面板**: http://127.0.0.1:8080
- **SillyTavern**: http://127.0.0.1:8000
- **API聚合端点**: http://127.0.0.1:8080/v1

## SillyTavern 配置

使用API聚合时，在SillyTavern中配置：
- **Custom Endpoint**: `http://127.0.0.1:8080/v1`
- **Custom API Key**: `daidaibird`

## 系统要求

- Node.js >= 18.0.0
- Git
- 约 500MB 存储空间（每个版本）

## 目录结构

```
~/st-launcher/       # 启动器目录
~/st-versions/       # SillyTavern各版本安装目录
```

## 手动安装

```bash
# 克隆仓库
git clone https://github.com/2830897438/st-launcher.git ~/st-launcher

# 进入目录
cd ~/st-launcher

# 启动
node launcher.js
```

## 常见问题

### Termux 中如何安装 Node.js?
```bash
pkg install nodejs git
```

### 端口被占用怎么办?
启动器会自动尝试清理占用的端口。如果失败，可以手动：
```bash
# 查找占用端口的进程
lsof -i:8000
# 杀死进程
kill -9 <PID>
```

### 如何更新启动器?
```bash
cd ~/st-launcher && git pull
```

## License

MIT

---

Made with ❤️ by Daidai Bird
